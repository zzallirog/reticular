# glados-portable — dependencies. Philosophy: MINIMAL, so it runs anywhere.
# Hooks and the slim extractor = pure python3 stdlib + urllib (NO pip packages needed at runtime).
#
# ── REQUIRED ──
# python3            >= 3.8   (stdlib: json, re, hashlib, urllib, glob, datetime)
# Claude Code        (claude-kit already installed — this kit EXTENDS it)
# ollama             (for recall-index embeddings)  → https://ollama.com
#   embed model:     embeddinggemma     (primary — multilingual, RU+EN+anything)
#                    OR nomic-embed-text (fallback, English-centric)
#   bootstrap.sh does it for you: ollama pull embeddinggemma
#
# ── OPTIONAL (offline eval / tuning only, NOT for hook runtime) ──
# numpy              — if you run build_embeddings.py in batch mode (otherwise pure python)
#   pip install numpy
#
# ── ENVIRONMENT CHECK (printed by bootstrap) ──
#   python3 --version
#   command -v ollama && ollama list
#   ls ~/.claude/projects/   # any logs available for slim extraction?
#
# Deliberately ABSENT: torch, transformers, sentence-transformers, langchain.
# Embedding goes through ollama HTTP (127.0.0.1:11434) — one process, portable, no GPU hell.
